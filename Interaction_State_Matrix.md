#### 3. `Interaction_State_Matrix` (交互状态矩阵 Skill)
**目的**：解决 AI 习惯性编写“理想路径 (Happy Path)”代码，而忽略加载中、空数据、错误状态等边缘情况，导致产品在实际运行中出现视觉缺失或用户体验中断的问题。

*   **约束核心**：**全状态覆盖 (Full State Coverage)** $\rightarrow$ 禁止在没有定义“异常态”的情况下交付 UI。
*   **AI 必须遵守的指令**：
    *   **原子组件状态矩阵**：每个交互式原子组件（如 Button, Input, Select）必须完整实现以下 5 种状态的样式定义：
        1.  `Default`：初始静止状态。
        2.  `Hover`：鼠标悬停（需有明显的亮度/色相变化）。
        3.  `Active`：点击按下（需有缩放或深色反馈，如 `scale(0.98)`）。
        4.  `Loading`：加载中（禁止禁用交互且无反馈，必须包含 Spinner 或进度条）。
        5.  `Disabled`：禁用状态（需有低对比度灰度处理，且 `cursor: not-allowed`）。
    *   **页面级状态矩阵**：每个核心页面/模块必须包含以下三种非理想状态的实现方案：
        1.  `Skeleton Screen` (骨架屏)：在数据请求期间，使用与最终布局一致的灰色占位块，禁止仅使用一个简单的 "Loading..." 文字。
        2.  `Empty State` (空状态)：当后端返回空数组时，必须提供包含【插图/图标 + 提示文字 + 引导操作按钮】的空状态页面。
        3.  `Error State` (错误页)：当请求失败或 404 时，必须提供【错误图标 + 错误描述 + 重试按钮】。
    *   **状态流转逻辑**：在代码实现中，必须使用明确的状态变量（如 `status: 'idle' | 'loading' | 'success' | 'error'`）来驱动 UI 切换，禁止使用大量混乱的布尔值。
*   **校验点**：
    *   **交付自检**：在交付页面代码前，AI 必须回答：*“该页面的加载状态如何体现？空数据状态如何呈现？错误处理如何跳转？”*
    *   **视觉自检**：检查所有可点击元素是否缺失 `:hover` 或 `:disabled` 样式。
    *   **强制交付**：交付物必须包含 `Loading` 和 `Empty` 两种状态的独立代码片段或条件渲染逻辑。
